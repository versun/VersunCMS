<% content_for :title, "New Redirect|Admin" %>

<div class="admin-form-container">
  <div class="form-header">
    <h1><i class="fas fa-plus-circle"></i> New Redirect</h1>
  </div>

  <%= render "form", redirect: @redirect %>

  <div class="form-actions-bottom">
    <%= link_to "Back to Redirects", admin_redirects_path, class: "btn btn-secondary" %>
  </div>
</div>
